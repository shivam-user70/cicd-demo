version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"

  build:
    commands:
      - echo "Nothing to build for static HTML"

  post_build:
    commands:
      - echo "Deploying to S3"
      - aws s3 cp . s3://your-bucket-name/ --recursive --exclude "buildspec.yaml"

artifacts:
  files:
    - index.html
